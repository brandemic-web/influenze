---
import { SIGNUP_URL } from "../../data/site";
import {
	HOW_IT_WORKS_EYEBROW,
	HOW_IT_WORKS_BUTTON,
	HOW_IT_WORKS_STEPS,
} from "../../data/howItWorks";
import iconSearch from "../../assets/images/common/icons/search.svg";
import iconEye from "../../assets/images/common/icons/eye.svg";
import iconRocket from "../../assets/images/common/icons/rocket.svg";
import Button from "../common/Button.astro";

const stepIcons: Record<string, ImageMetadata> = {
	search: iconSearch,
	eye: iconEye,
	rocket: iconRocket,
};
---

<section aria-labelledby="how-it-works-heading" class="-mt-20 overflow-hidden relative z-5">
	<div class="relative mx-auto flex flex-col items-center px-6 text-center">
		<p class="text-[1rem] lg:text-[1.125rem] uppercase leading-[normal] text-brand-primary">{HOW_IT_WORKS_EYEBROW}</p>
		<h2
			id="how-it-works-heading"
			class="mt-5 lg:mt-8 max-w-130 text-[1.875rem] capitalize leading-[normal] text-white lg:text-[2.25rem] [text-box:trim-both_cap_alphabetic]"
		>
			Take your brand further with the <span class="text-brand-primary">right creators</span>
		</h2>

		<!-- Mobile: full-bleed GSAP ticker (cards rendered twice for a gapless loop). Desktop: static centered row. -->
		<div
			class="mt-8 -mx-6 w-[calc(100%+3rem)] overflow-x-auto overscroll-x-contain lg:mx-0 lg:w-full lg:overflow-visible"
			data-marquee
		>
			<div
				class="flex w-max flex-row items-center justify-start gap-3 px-6 lg:w-full lg:gap-5 lg:justify-center lg:items-stretch lg:px-0"
				data-marquee-track
				data-reveal
				data-reveal-stagger="0.15"
			>
				{
					[...HOW_IT_WORKS_STEPS, ...HOW_IT_WORKS_STEPS].map((step, i) => (
						<article
							class:list={[
								"rounded-sm bg-linear-to-r from-[#19123A] via-[#19123A] to-[#575757] p-px",
								i >= HOW_IT_WORKS_STEPS.length && "hidden",
							]}
							data-marquee-clone={i >= HOW_IT_WORKS_STEPS.length ? "" : undefined}
							aria-hidden={i >= HOW_IT_WORKS_STEPS.length ? "true" : undefined}
							data-reveal-item
						>
							<div class="flex min-h-47 h-full items-center rounded-sm bg-background-primary px-6 py-7 lg:px-15 lg:py-10">
								<div class="flex w-75 max-w-85.75 lg:w-82.75 lg:max-w-full flex-col gap-5 text-left">
									<div class="flex items-center gap-4">
										<img src={stepIcons[step.icon].src} alt="" class="h-12 w-12 rounded-sm" loading="lazy" />
										<h3 class="text-[1.25rem] lg:text-md capitalize leading-[normal] text-white [text-box:trim-both_cap_alphabetic]">{step.title}</h3>
									</div>
									<p class="text-[0.875rem] lg:text-[1.125rem] leading-6.5 text-[#CECECE]/70 [text-box:trim-both_cap_alphabetic]">{step.description}</p>
								</div>
							</div>
						</article>
					))
				}
			</div>
		</div>

		<Button href={SIGNUP_URL} variant="nav" class="mt-8 lg:mt-12">
			{HOW_IT_WORKS_BUTTON}
		</Button>
	</div>
</section>

<script>
	import "../../scripts/reveals";
	import "../../scripts/marquee";
</script>
