---
/**
 * Results surface before a search has run — `analyze/widgets/analyze_empty_state.dart`.
 */
import Icon from "../../../common/Icon.astro";

/**
 * Both tiles, in the app's order.
 */
const options = [
	{
		icon: "profileSearch",
		title: "Search By Profile",
		subtitle: "Find profiles by their username / handle\nacross selected social platforms",
	},
	{
		icon: "parameterMagic",
		title: "Parameter Magic",
		subtitle: "Discover profiles by going broader with\nour intelligent parameters & filters",
	},
] as const;
---

<div class="absolute inset-0 flex flex-col items-center justify-center" data-wf-empty>
	{/*
		`emptyStateTitle` at the call site's own 24 — the app's copyWith. The platform
		is interpolated (`Start your $platform Search`), so it tracks the rail's
		selection; Instagram is what the story has picked.
	*/}
	<p class="text-md text-wf-text">Start your Instagram Search</p>
	{/* `emptyStateSubtitleSecondary` — 12/400 on lavender17 */}
	<p class="mt-3 text-center text-[0.75rem] leading-normal text-[#bfabff]">
		A few common ways to begin, use the sidebar to refine the results more
	</p>

	<div class="mt-12 flex items-start justify-center gap-4">
		{
			options.map((option) => (
				<div class="flex w-fit flex-col rounded-[1.5rem] bg-[#2c2c44] p-6">
					<Icon name={option.icon} size="2rem" class="text-wf-green-bright" />
					{/* `searchOptionTileTitle` — 16/400 */}
					<p class="mt-4 text-[1rem] whitespace-nowrap text-wf-text">{option.title}</p>
					{/* `searchOptionTileSubtitle` — 10/400 on lavender95 */}
					<p class="mt-3 text-[0.625rem] leading-normal whitespace-pre-line text-[#7878a0]">
						{option.subtitle}
					</p>
				</div>
			))
		}
	</div>

	{/*
		`WalkthroughLink` — `SizedBox(24)`, then a 14px glyph, gap 4, and a 12/400
		`green8` label underlined in the same green. It opens the product demo video,
		and is gated on `demoVideoUrlProvider` having a URL, so it is remote-config'd
		rather than always present.
	*/}
	<span class="mt-6 flex items-center gap-1">
		<Icon name="walkthrough" size="0.875rem" class="text-wf-green-bright" />
		<span class="text-[0.75rem] whitespace-nowrap text-wf-green-bright underline decoration-wf-green-bright">
			Take a walkthrough of all the features!
		</span>
	</span>
</div>
