# Sanity project — get these from https://sanity.io/manage
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production

# Origin the Presentation tool iframes to preview the site. Defaults to the
# local dev server if unset.
PUBLIC_SITE_ORIGIN=http://localhost:1234

# Server-only — a Viewer-permission token from sanity.io/manage → API →
# Tokens. NEVER prefix this with PUBLIC_: it must never reach the browser.
# Required for the draft-mode preview (see src/pages/api/draft-mode/enable.ts).
SANITY_API_READ_TOKEN=
